name: ensemble_learning

conda_env: conda.yaml

entry_points:
  stacking_train:
    parameters:
      dataset: {type: str}
    command: "spark-submit EnsembleStacking.py -t {dataset}"
  stacking_prediction:
    parameters:
      dataset: {type: str}
    command: "spark-submit EnsembleStacking.py -p {dataset}"
  main:
    parameters:
      maxIter: {type: int, default: 5}
    command: "spark-submit main.py {maxIter}"